<li>
	<p class="name"><%=document.titol%></p>
	<%=link_to(excerpt(document.document_file_name, '.', :radius => 15),document.document.url,:class => 'file')%>
	<%if session[:professor_id]%>
	<%=link_to '',delete_document_url(@cas,document),:class => 'remove', :confirm => "Desea eliminar este material?"%>
	<%end%>
</li>